#!/bin/bash --login

LIB_PATH=$( dirname $0 )/../lib/script
source $LIB_PATH/script_helper.sh


rm --recursive --force $TMP_PATH
rm --recursive --force $( ls --directory $PKG_PATH/*/ )

rm --force $ROOT_PATH/.configured
exit $EXITCODE_SUCCESS
